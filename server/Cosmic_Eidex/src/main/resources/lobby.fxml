<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ListView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<AnchorPane prefHeight="600.0" prefWidth="800.0" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="cosmic.eidex.gui.ControllerFX.LobbyController">

    <VBox alignment="TOP_CENTER" spacing="10" AnchorPane.leftAnchor="10" AnchorPane.rightAnchor="10" AnchorPane.topAnchor="10">
      <HBox prefHeight="35.0" prefWidth="780.0" spacing="550.0">
         <children>
              <Label prefHeight="29.0" prefWidth="146.0" style="-fx-font-size: 24px; -fx-font-weight: bold;" text="LOBBY" />
            <Label fx:id="loggedinplayer" prefHeight="17.0" prefWidth="166.0" text="Angemeldeter Spieler" />
         </children>
      </HBox>

        <HBox alignment="CENTER" spacing="20">
            <VBox spacing="5">
                <Label text="Aktive Räume" />
                <ListView fx:id="raumListe" prefHeight="150.0" prefWidth="250.0" />
            <HBox prefHeight="18.0" prefWidth="246.0" spacing="10.0">
               <children>
                  <Button onAction="#handleBeitreten" text="Beitreten" />
               </children>
            </HBox>
            </VBox>

            <VBox spacing="5">
                <Label text="Mitspieler" />
                <ListView fx:id="spielerListe" prefHeight="150.0" prefWidth="250.0" />
            </VBox>

            <VBox spacing="5">
                <Label text="Bestenliste" />
                <ListView fx:id="bestenliste" prefHeight="150.0" prefWidth="250.0" />
            </VBox>
        </HBox>

        <HBox alignment="CENTER" spacing="10" />

        <Label text="Chat (Global)" />
        <ListView fx:id="chatListView" editable="false" prefHeight="150.0" />
        <HBox spacing="10">
            <TextField fx:id="chatInput" promptText="Nachricht schreiben..." HBox.hgrow="ALWAYS" />
            <Button onAction="#handleSendeNachricht" text="Senden" />
      
           <HBox alignment="CENTER" spacing="10">
               <Button onAction="#handleSpielErstellen" text="Spielraum erstellen" />
               <Button onAction="#handleSpielregeln" text="Spielregeln" />
               <Button onAction="#handleProfilBearbeiten" text="Profil bearbeiten" />
            <Button layoutX="231.0" layoutY="10.0" onAction="#handleAbmelden" text="Abmelden" />
           </HBox>
        </HBox>
    </VBox>
</AnchorPane>
